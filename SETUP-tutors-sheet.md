# 講師検索ページ（tutors.html）とGoogleフォームの接続手順

講師応募フォームの回答を、AIの補助を受けつつ整えて、`tutors.html` の条件検索に反映します。
**個人情報を守るため、スプレッドシートは2つに分けて運用します。**

## 全体の流れ

```
講師が応募フォームに回答
      ↓  回答スプレッドシート【非公開】に1行追加
      ↓  ★スクリプトが自動整形（選択式はそのままコード化・AIが地域/学校歴/コメントを補助）
      ↓  運営が中身を確認 → 【評価】を手入力・【公開】をTRUE
      ↓  メニュー「KKM → 公開用シートへ同期」
   公開用スプレッドシート【リンク閲覧可】に、公開OKな項目だけコピー
      ↓
   tutors.html が公開用シートを読み込んで表示
```

- **ほとんどの項目は自動**。運営がやるのは「評価の入力」と「公開の切替」だけ。
- 評価4.0未満・公開OFFの間はサイトに出ません。

---

## 2つのスプレッドシート

| | ① 回答シート | ② 公開用シート |
|---|---|---|
| 中身 | フォーム回答（氏名・メール含む）＋整形後の列＋評価 | 公開OKな項目だけ（**評価は含めない**） |
| 共有 | **非公開のまま** | **リンクを知っている全員＝閲覧者** |
| サイトが読む | ✕ | **◯（このIDを貼る）** |

---

## 1. 回答シートに「出力用の列」を追加する

フォーム回答シートの右側の空き列に、次の見出しを1行目に追加してください。
**フォームの質問列（左側）はそのままでOK**。スクリプトが自動で読み取って、この列を埋めます。

`ニックネーム` `学部学年` `対応サービス` `希望時給` `指導志望学年` `指導科目`
`都道府県` `出身校` `部活・習い事` `受験経験` `自己PR` `目指す姿` `コメント`
`出身地` `きょうだい` `幼少期` `学校歴` `学習スタイル` `得意不得意` `関わり方`
`コミュニケーション` `やる気スイッチ` `AI処理日時` `評価` `公開`

- **手入力するのは `評価` と `公開` だけ**
  - `評価`：10基準の平均（4.0以上で掲載）
  - `公開`：掲載してよければ `TRUE`
- それ以外はすべて**スクリプトが自動で記入**します。

> ⚠️ これらの出力列は**1つずつ**にしてください（同じ見出しが2つあると同期がずれます）。

### 何がどうやって埋まるか

| 出力列 | 元になるフォームの質問 | 方法 |
|---|---|---|
| ニックネーム | ニックネーム | 転記 |
| 学部学年 | 学部・学年 | 転記（神戸大 ○学部 ○年） |
| 対応サービス | 「興味があるのはどれ」 | 家庭教師→tutor / 教育・受験相談→consult（両方可） |
| 希望時給 | 希望時給 | 転記 |
| 指導志望学年・指導科目 | 指導志望学年・指導可能科目 | 転記 |
| 都道府県 | 出身地（都道府県） | 転記 |
| 部活・習い事 / 自己PR / 目指す姿 | 各記述式 | 転記 |
| 受験経験 | 推薦・浪人・編入経験 | 転記 |
| きょうだい | 兄弟構成 | コード化（first/middle/last/only） |
| 幼少期 | 幼少期の過ごし方 | コード化（lessons/free） |
| 学習スタイル | 中高時代の勉強スタイル | コード化（self/cram） |
| 得意不得意 | 勉強について | コード化（effort/inquiry） |
| 関わり方 | 指導スタイル | コード化（companion/leader/両方） |
| コミュニケーション | コミュニケーションスタイル | コード化（empathy/logical） |
| やる気スイッチ | やる気の引き出し方 | コード化（competition/curiosity/safety） |
| 出身地（地域） | 都道府県＋引っ越し回数 | **AIが判定**（local/city/transfer） |
| 学校歴 | 出身中学高校・入試種別 | **AIが判定**（public/jhs/hs） |
| 出身校 | 出身中学高校 | **AIが公私のみ要約**（学校名は出しません） |
| コメント | 性格・自己PR・目指す像 | **AIが語り口調で生成** |

> AIが判定した列（出身地・学校歴・出身校・コメント）は、内容を見て手で直してもOKです。

---

## 2. AI補助を設定する（Apps Script）

スクリプト本体は **`apps-script/tutors-classify.gs`**。

1. 回答シートで「拡張機能 → Apps Script」を開き、中身を貼り付けて保存
2. [Google AI Studio](https://aistudio.google.com/app/apikey) でAPIキーを発行
   （※大学アカウントは無料枠が0のことが多いので、**個人のGmail**で発行するのが確実）
3. 「プロジェクトの設定 → スクリプト プロパティ」に `GEMINI_API_KEY` を登録
4. `CONFIG.PUBLIC_SPREADSHEET_ID` に、次で作る公開用シートのIDを入れる
5. 関数 `installTrigger` を1回実行 → 以後、フォーム送信で自動整形
6. 既存の回答は「メニュー KKM → 選択行をAI分類 / 未処理をまとめて分類」

---

## 3. 公開用シートを作って共有する

1. 新しいスプレッドシートを作成（例「KKM公開用」）
2. URLの `/d/` と `/edit` の間が **ID**
   → Apps Scriptの `CONFIG.PUBLIC_SPREADSHEET_ID` と、`tutors.html` の `CONFIG.SHEET_ID` の**両方**に貼る
3. 公開用シートを **「共有」→「リンクを知っている全員」→「閲覧者」** に（回答シートは非公開のまま）

```js
// tutors.html 上部
var CONFIG = { SHEET_ID: '公開用シートのID', SHEET_NAME: '公開用', SHEET_GID: '' };
```

---

## 4. 日々の運用（応募が来たら）

1. スクリプトが自動で各列を記入（`AI処理日時` が入る）
2. 中身を確認し、**`評価` を入力**・掲載OKなら **`公開` を TRUE**
3. メニュー **「KKM → 公開用シートへ同期」**
   → 評価4.0以上・公開TRUEの人だけがサイトに反映

---

## サイトに表示される公開項目

ニックネーム／学部・学年／対応サービス／希望時給（家庭教師のみ）／出身地（都道府県）／
出身校（公私のみ）／指導志望学年・指導科目／部活・習い事／受験経験（推薦・浪人・編入）／
自己PR／目指す姿／ひとことコメント／価値観タグ（検索用）

> 氏名・メール・学生証・最寄り駅・連絡先などは**公開用シートに含めず、サイトにも出ません**。

## うまくいかないとき

- サイトが赤帯 → 公開用シートの共有が「リンクを知っている全員／閲覧者」か確認
- 先生が出ない → `評価`4.0以上・`公開`TRUE・同期実行済みかを確認
- 項目が空 → その出力列の見出しが正しいか、`AI処理日時`が入っているか確認
- AIが `429 / limit: 0` → **個人GmailのAPIキー**に差し替える（大学アカウントは無料枠0のことが多い）
- 同期しても出ない → 出力列の見出しが**重複していないか**確認（重複すると空の方を読むことがある）

## プライバシー面の注意

- 公開用シートに個人情報の列を作らない（スクリプトは公開OK列だけ写します）
- 応募フォームに「匿名プロフィールの公開に同意する」チェックを用意しておく
